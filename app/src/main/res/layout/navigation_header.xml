<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerHorizontal="true">

    <ImageView
        android:id="@+id/navigation_background"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:src="@drawable/header_nav"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"/>

    <ImageView
        android:id="@+id/navigation_avatar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/default_avatar"
        android:layout_alignRight="@id/navigation_txt_name"
        android:layout_alignLeft="@id/navigation_txt_name"
        android:adjustViewBounds="true"
        android:layout_above="@id/navigation_txt_name"
        android:layout_marginBottom="@dimen/standard_margin"
        />

    <TextView
        android:id="@+id/navigation_txt_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="علی مقدم"
        android:textSize="@dimen/big_text_size"
        android:textColor="@color/white"
        android:layout_above="@id/navigation_txt_email"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/standard_margin_2x"
        android:layout_marginBottom="@dimen/standard_margin"/>

    <TextView
        android:id="@+id/navigation_txt_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="alimoghadam75@gmail.com"
        android:textSize="@dimen/normal_text_size"
        android:textColor="@color/white"
        android:layout_alignParentRight="true"
        android:layout_alignBottom="@id/navigation_background"
        android:layout_marginRight="@dimen/standard_margin_2x"
        android:layout_marginBottom="@dimen/standard_margin_2x"/>

</RelativeLayout>